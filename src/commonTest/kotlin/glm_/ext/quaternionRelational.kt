package glm_.ext

import glm_.quat.Quat
import kotlin.test.Test

class quaternionRelational {

    @Test
    fun equal() {

//        val q = Quat(1, 0, 0, 0)
//        quaType const P(1, 0, 0, 0)
//        Error += glm::all(glm::equal(Q, P, glm::epsilon<float>())) ? 0 : 1
    }
}